
import React from 'react';
import { View, Text } from 'react-native';

const Home = (props) => {

    props = props.props;

    return (
        <View >
            <Text > alou </Text>
        </View>
    );
}

export default Home;